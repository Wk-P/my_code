# Revised Introduction / 引言修改版

This document provides a highly polished, academically rigorous revision of the **Introduction** section for your paper, incorporating your core thesis: **ILP is used strictly as an offline baseline/oracle to establish the theoretical performance limit under standard resource constraints, while Safe RL serves as the real-time online controller that seamlessly scales to handle complex privacy-preserving constraints without suffering from the "mathematical formulation hell" of classical programming.**

---

## 1. English Version (英文版)

### 1. Introduction
With the rapid proliferation of distributed edge computing paradigms, executing computation-heavy and delay-sensitive application services closer to end-users has become the standard operational design [5, 6]. In multi-tenant edge environments, various software services are dynamically deployed on shared Edge Computing Units (ECUs). However, when services handling conflicting, sensitive, or competitive data are co-located on the same physical host, shared hardware resources and side-channel vulnerabilities create a significant risk of privacy leaks [13]. Consequently, edge service provisioning is no longer merely a resource efficiency optimization problem, but also a safety-critical privacy-preserving challenge: the orchestrator must dynamically place services to satisfy capacity bounds while strictly preventing conflicting service pairs from being co-located on the same server [37]. **This work formulates the problem as a single-objective optimization under two hard constraints, not a multi-objective trade-off**: the sole optimization objective is to maximize resource utilization (AR), subject to (i) a capacity constraint and (ii) a privacy-conflict constraint, both of which must hold with zero violation.

The classical approach to resolving service placement relies heavily on mathematical programming, such as Integer Linear Programming (ILP) and Mixed-Integer Linear Programming (MILP) [9, 10]. In offline, perfect-information scenarios, ILP is the exact-optimization reference method, computing mathematically optimal deployment configurations. In practical edge deployment settings, however, the ECU set and the service set for a given scenario are fixed and known in advance, but the orchestrator must commit to each service's placement one at a time, in sequence, and each decision, once made, cannot be revisited or revised. **This work uses ILP's offline exact solution as the comparison baseline against which RL and Safe RL are evaluated**, defining the theoretical ceiling of resource utilization under standard capacity constraints, without ILP participating in the online decision process itself. Benchmarking a Safe RL controller against this baseline reveals a stable **0.10-to-0.12 structural gap in resource utilization**, which constitutes the upper bound achievable under an online, causal, single-step decision paradigm; this gap quantifies the competence of Safe RL as an online executor relative to the offline optimum.

The environment further incorporates non-negotiable **privacy-preserving co-location constraints**, and this is where a structural advantage of Safe RL over classical mathematical programming becomes apparent: **the tractability of formulating complex, non-linear constraints**. Encoding state-dependent, multi-tenant co-location privacy conflicts into an ILP formulation requires linearizing complex conditional logic through a large number of binary helper variables and Big-M constants, which substantially increases the NP-hard problem's solving complexity and degrades solver tractability. The proposed Safe RL framework (EdgePro), by contrast, decouples the safety-enforcement mechanism from the underlying optimization mathematics. A lightweight, state-dependent **Action Masking** mechanism implemented directly in the simulator environment dynamically filters out privacy-violating placement actions, guaranteeing a deterministic **zero-violation privacy co-location boundary** at runtime without requiring any algebraic modification of the optimization formulation.

A systematic evaluation of diverse learning architectures — masked PPO, Lagrangian dual PPO, and standard DQN/DDQN — clarifies two distinct levels of learning-based service placement. First, regarding reward shaping and strategy learning: continuous, quality-graded reward signals, rather than binary success/failure rewards, are the primary driver of strategy convergence; under graded rewards, standard unconstrained RL models learn valid placement policies with success rates rising from zero to approximately 30%. Second, regarding constraint enforcement and safety boundaries: reward shaping determines whether a policy can be learned at all, but it does not substitute for dedicated safety mechanisms in enforcing the privacy boundary. Action Masking structurally eliminates invalid actions and guarantees a zero-violation rate, whereas Lagrangian dual methods provide only soft, probabilistic boundary control.

In summary, this work connects theoretical optimality with real-world deployability: the offline ILP solution is retained as the evaluation baseline, and Safe RL's constraint-formulation tractability is leveraged to enforce privacy boundaries without mathematical-formulation overhead, yielding a deployable design pattern for secure, edge-native service placement.

---

## 2. Chinese Version (中文版)

### 一、 引言 (Introduction)
随着分布式边缘计算范式的快速普及，在更靠近终端用户的节点执行高计算量且时延敏感的应用服务，已成为标准的系统设计模式 [5, 6]。在多租户边缘环境中，多种软件服务通常需要动态部署在共享的边缘计算单元（Edge Computing Unit, ECU）上。然而，当处理存在利益冲突、敏感或竞争性数据的多个服务被共置于同一台物理主机时，共享硬件资源和侧信道漏洞将带来巨大的隐私泄露风险 [13]。因此，边缘服务供给已不仅仅是一个单纯的资源效率优化问题，更是一个关乎安全的隐私保护挑战：服务编排器必须在满足容量约束的同时，严格避免将存在隐私冲突关系的服务对共置于同一台服务器上 [37]。**本文将该问题形式化为一个优化目标、两条硬性约束的单目标优化问题,而非多目标权衡**:唯一的优化目标是最大化资源利用率(AR),该目标需在容量约束与隐私冲突约束这两条硬约束下实现,二者必须零违反,不是与优化目标相互权衡的另外两个目标。

解决服务放置问题的经典方法高度依赖于数学规划，例如整数线性规划（ILP）和混合整数线性规划（MILP）[9, 10]。在离线且信息完备的场景下，ILP 是精确求解的参照方法，能够计算出数学上的全局最优部署配置。而在边缘计算的实际部署场景中，对于给定场景，ECU 集合与服务集合（SVC 集合）从一开始就是固定且完整已知的，但编排器必须逐个服务、按顺序依次做出放置决策，且每一次决策一旦执行便不可回溯或修改。**本文以 ILP 的离线精确解作为评估基准，RL 与 Safe RL 的表现均以此为对比对象**，用以标定标准容量约束下系统资源利用率的理论上界，ILP 本身不参与在线决策过程。以此基准衡量 Safe RL 控制器，可得到一个稳定的 **0.10 至 0.12 的资源利用率结构性差距**，这一差距即在线、因果、单步决策范式下可达到的性能上限，量化了 Safe RL 作为在线执行器相对离线最优解的能力边界。

在此基础上，环境进一步纳入**不可妥协的隐私保护共置约束**，Safe RL 相较于传统数学规划（如 ILP）的一项结构性优势由此显现：**面对复杂非线性约束时的建模可行性**。将与状态相关的多租户共置隐私冲突纳入 ILP 建模，需要通过大量二值辅助变量和 Big-M 常数对复杂条件逻辑进行线性化处理，这会显著增加 NP-hard 问题的求解复杂度并降低求解器的可行性。本文提出的 Safe RL 框架（EdgePro）将安全执行机制与底层优化数学解耦：在仿真环境中直接实现轻量级、状态相关的**动作掩码（Action Masking）**，在运行期动态过滤所有导致隐私冲突的部署动作，在无需对优化建模做任何代数修改的前提下，保证隐私共置边界的确定性**零违规**。

对多种学习架构（带掩码的 PPO、拉格朗日对偶 PPO、基础 DQN 与 DDQN）的系统性评估，澄清了学习型服务放置中两个不同层面的问题。其一，就奖励塑造与策略学习而言：按分配质量连续分级的奖励信号，而非二元的成败奖励，是策略收敛的主要驱动因素；在分级奖励下，不具备约束机制的普通 RL 模型也能学出有效的放置策略，成功率从零提升至约 30%。其二，就约束执行与安全边界而言：奖励塑造决定的是策略能否学得动，并不能替代专用安全机制对隐私边界的守护——动作掩码在结构上排除违规动作，保证隐私零违规；拉格朗日对偶提供的则是软性的、概率意义上的边界控制,不构成结构性保证。

综上，本文将理论最优性与实际可部署性相连接：离线 ILP 的精确解作为评估基准被保留下来，Safe RL 在复杂约束下的建模可行性被用于在无需额外数学建模开销的前提下执行隐私边界，二者共同构成一种面向安全、边缘原生服务部署的可落地设计模式。
