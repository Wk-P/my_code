# Experiment

All experiments have two violation:
1. Capacity violation
- Because of ecu capacity vms sum > svc required vms sum, this capacity violation is for one service when placing on the ECU selected
2. Safe (privacy conflict) violation

P3 problem experiment not have safe constraint, so p3 experiment can violate the No.2 violation